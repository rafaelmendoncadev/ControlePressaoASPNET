@model ControlePressao.Models.LoginViewModel
@{
    ViewData["Title"] = "Login";
    Layout = "~/Views/Shared/_LayoutPublic.cshtml";
}

<div class="auth-card animate-fade-in-up">
    <div class="auth-header">
        <div class="auth-logo">
            <i class="fas fa-heartbeat"></i>
        </div>
        <h1 class="auth-title">Bem-vindo de volta</h1>
        <p class="auth-subtitle">Entre na sua conta para continuar</p>
    </div>
    
    <div class="auth-body">
        <form asp-action="Login" asp-route-returnurl="@ViewData["ReturnUrl"]" method="post" class="auth-form">
            <div asp-validation-summary="All" class="alert alert-danger alert-modern mb-4" role="alert" style="display: none;"></div>
            
            <div class="form-floating mb-3">
                <input asp-for="Email" class="form-control modern-input" placeholder="Digite seu email" />
                <label asp-for="Email" class="form-label">
                    <i class="fas fa-envelope me-2"></i>Email
                </label>
                <span asp-validation-for="Email" class="invalid-feedback"></span>
            </div>
            
            <div class="form-floating mb-3">
                <input asp-for="Password" class="form-control modern-input" placeholder="Digite sua senha" type="password" />
                <label asp-for="Password" class="form-label">
                    <i class="fas fa-lock me-2"></i>Senha
                </label>
                <span asp-validation-for="Password" class="invalid-feedback"></span>
            </div>
            
            <div class="form-check mb-4">
                <input asp-for="RememberMe" class="form-check-input modern-checkbox" />
                <label asp-for="RememberMe" class="form-check-label">
                    Lembrar de mim
                </label>
            </div>
            
            <button type="submit" class="btn btn-primary btn-modern w-100 mb-3">
                <i class="fas fa-sign-in-alt me-2"></i>
                Entrar
            </button>
        </form>
        
        <!-- Botão de acesso rápido para teste -->
        <div class="test-user-section mb-3">
            <button type="button" class="btn btn-outline-info btn-modern w-100" onclick="fillTestUser()">
                <i class="fas fa-user-cog me-2"></i>
                Acesso Rápido - Usuário Teste
            </button>
            <small class="text-muted d-block text-center mt-1">
                Email: teste@teste.com | Senha: 123456
            </small>
        </div>
        
        <div class="auth-divider">
            <span>ou</span>
        </div>
        
        <div class="auth-footer">
            <p class="text-center mb-0">
                Não tem uma conta? 
                <a asp-action="Register" class="auth-link">Cadastre-se aqui</a>
            </p>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        function fillTestUser() {
            // Preencher os campos com as credenciais do usuário teste
            document.getElementById('Email').value = 'teste@teste.com';
            document.getElementById('Password').value = '123456';
            
            // Adicionar efeito visual para indicar que os campos foram preenchidos
            const emailField = document.getElementById('Email');
            const passwordField = document.getElementById('Password');
            
            emailField.classList.add('is-valid');
            passwordField.classList.add('is-valid');
            
            // Remover a classe após alguns segundos
            setTimeout(() => {
                emailField.classList.remove('is-valid');
                passwordField.classList.remove('is-valid');
            }, 2000);
            
            // Focar no botão de submit
            document.querySelector('button[type="submit"]').focus();
        }
    </script>
}
